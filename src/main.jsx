import React from "react";
import ReactDOM from "react-dom/client";
import FormWithPopup from "./FormWithPopup";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FormWithPopup />
  </React.StrictMode>
);
